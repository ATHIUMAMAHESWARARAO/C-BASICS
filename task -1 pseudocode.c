#include<stdio.h>
void main()
{
	int p, q, r;
    printf("enter a number");
	scanf("%d%d%d", &p, &q, &r); 
	if((r>p) || (7-7)>(r-7))
	{
		 q=p;
	}
	else
	{
		 p=12;
	}
		printf("the answer is %d", p+q+r);
}
